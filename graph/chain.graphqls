extend type Query {
  validators: [String!]!
}

extend type Mutation {
  start_epoch: Boolean!

  faucet(
    """ 调用方地址（32 字节 hex 或 SS58）"""
    caller: String!
    """ 地址类型，0 => TEE, 1 => polkadot_sign """
    callerType: Int!
  ): Boolean!
}
